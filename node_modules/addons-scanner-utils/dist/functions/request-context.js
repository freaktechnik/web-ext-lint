"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestIdMiddleware = exports.withRequestIdHeader = exports.getRequestId = exports.requestContextStorage = exports.AMO_REQUEST_ID_HEADER = void 0;
const node_async_hooks_1 = require("node:async_hooks");
const node_crypto_1 = __importDefault(require("node:crypto"));
/**
 * Name of the header carrying the unique request ID issued by AMO.
 */
exports.AMO_REQUEST_ID_HEADER = 'X-AMO-Request-ID';
exports.requestContextStorage = new node_async_hooks_1.AsyncLocalStorage();
/**
 * Return the `X-AMO-Request-ID` of the request currently being handled, or
 * `undefined` when there is no active request context (e.g. outside of the
 * Express middleware chain).
 */
const getRequestId = () => { var _a; return (_a = exports.requestContextStorage.getStore()) === null || _a === void 0 ? void 0 : _a.requestId; };
exports.getRequestId = getRequestId;
/**
 * Return a copy of `headers` with the `X-AMO-Request-ID` header added when a
 * request ID is available in the current context. When no request ID is in
 * scope, `headers` is returned unchanged.
 */
const withRequestIdHeader = (headers = {}) => {
    const requestId = (0, exports.getRequestId)();
    return requestId
        ? Object.assign(Object.assign({}, headers), { [exports.AMO_REQUEST_ID_HEADER]: requestId }) : headers;
};
exports.withRequestIdHeader = withRequestIdHeader;
/**
 * Express middleware that captures the AMO request ID from the incoming
 * `X-AMO-Request-ID` header (generating a UUID when it is absent), exposes it
 * on `req.amoRequestId`, and runs the rest of the middleware chain inside a
 * request context so the ID stays available for the duration of the request
 * and can be echoed back on every outgoing AMO API call.
 */
const requestIdMiddleware = (req, res, next) => {
    var _a;
    const requestId = (_a = req.get(exports.AMO_REQUEST_ID_HEADER)) !== null && _a !== void 0 ? _a : node_crypto_1.default.randomUUID();
    req.amoRequestId = requestId;
    exports.requestContextStorage.run({ requestId }, () => next());
};
exports.requestIdMiddleware = requestIdMiddleware;
